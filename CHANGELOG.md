# Changelog

## 3.3.23

- 人设指令：新增 `/查看人设`，可查看指定或当前人设的详情与全部参考图；新增管理员 `/修改人设`，可修改现有人设名称或基础描述。
- 副脑兼容：新增“使用 AstrBot 当前文本模型”开关，开启后通过 AstrBot SDK 调用当前 Chat Provider，避免向完整生图 endpoint 错误拼接 `/v1/chat/completions`。
- 自定义接口：JSON 附加参数会恢复合法的布尔、数字、数组、对象和 null 类型，普通文本保持字符串。
- 异步图片：`custom_endpoint` 检测到嵌套 `task_id` 后，会轮询同域名 `/api/tasks/{task_id}` 获取最终图片。
- AstrBot 4.26 兼容：LLM 生图、自拍和视频工具会从 `ContextWrapper.context.event` 解包原始消息事件，修复结果无法下发的问题。
- 异步视频：修复 `async_task` 被误判为 `openai_sync`；支持从 `data` 列表提取 task_id、状态并轮询 `/api/tasks/{task_id}`。Issue #53 为 #52 的重复报告，共用同一修复。
- 测试：增加人设指令、AstrBot 副脑、参数类型、图片异步任务、ContextWrapper 和视频异步任务回归覆盖。

## 3.3.22

- 性能优化：所有生图 Provider（标准生图、Chat 透传、Gemini 官方、自定义完整路径）与视频节点在读取本地参考图、Base64 编解码时不再阻塞事件循环，改为线程池异步执行；多参考图或高并发场景下机器人整体响应更流畅。
- 稳定性增强：各通道下载网络参考图统一补充超时与浏览器 UA，避免个别参考图源长时间无响应时拖垮整个生成请求。
- 代码质量：抽出统一的参考图读取助手（`fetch_reference_bytes` / `fetch_reference_data_url`），消除四个 Provider 中重复的下载/读盘/编码逻辑，并清理无用死代码。
- 性能优化：AstrBot 原生配置热同步改为先比对文件修改时间（mtime），未变更时跳过整文件读取与 SHA-256 计算，降低每条指令的固定开销。
- 修复：副脑内置风格配置项与实际风格矩阵不一致——原生配置页此前的“电影感写真 / 商业产品摄影 / 二次元精修”并不存在，选择后会静默回退到默认风格；现已修正为“电影级光影大片 / 日系插画大师 / 3D 潮玩盲盒”，并补充回归测试锁定 schema、代码与配置页三方一致。
- 配置页体验：修正过时的静态资源缓存版本号，更新插件后不再命中旧的 JS/CSS；存在未保存修改时离开或刷新页面会弹出确认，避免误丢配置。

## 3.3.21

- 修复 `/画`、极速宏预设、`/自拍` 和 `/视频` 在发送“开始生成”提示后，可能被其他插件的 `after_message_sent` 处理链终止而不再继续生成的问题。
- 优化命令生成链路：等待提示改为通过 AstrBot 事件发送接口主动下发，生成链和后台视频任务会继续执行；最终成功与失败结果仍按原流程返回。
- 补充覆盖极速宏预设、画图、自拍和视频指令的回归测试，避免等待提示再次阻断后续生成。

## 3.3.20

- 新增节点级默认尺寸配置：每个图像/视频节点都可以单独设置默认分辨率尺寸；调用时显式传入 `size` 或 `resolution` 会优先覆盖节点默认值，备用节点切换时会使用备用节点自己的默认尺寸。
- 修复极速宏预设在 AstrBot 自定义指令前缀下无法触发的问题：兼容 AstrBot 已剥离前缀后的 `is_at_or_wake_command` 事件，确保 `指令前缀+预设名` 可直接触发。
- 优化预设触发体验：按最长预设名优先匹配，支持预设后追加规则，并解析预设提示词与追加规则里的 `--参数`。
- 完善预设配置兼容性：支持全角冒号 `：` 分隔预设名和提示词，并保留 `/`、`!`、`！`、`.` 旧触发前缀兼容。

## 3.3.19

- 新增面向其他插件的“返回式生图”能力：`generate_image` 函数工具在显式传入 `return_result=true` 时不再自动下发图片，而是返回包含图片 URL/本地路径/元数据的 JSON；默认行为保持原样。
- 新增 Web API `POST /astrbot_plugin_omnidraw/generate_image_for_plugin`，便于其他插件通过 HTTP 调用万象画卷生图并获取图片结果。
- 返回式生图支持自拍模式：现有 `generate_selfie(return_result=true)` 会返回图片 JSON，并按自拍链路与当前人设参考图生成。
- 优化返回式生图稳定性：异常场景保持 JSON 返回，结果聚合不再因提示词与结果数量不一致而漏掉成功图片。
- 加强安全与可用性：返回给调用方的错误会自动脱敏 API Key、Bearer Token 和图片 Base64；参考图支持 JSON 对象入参、自动去重，并对本地参考图增加大小限制保护。

## 3.3.18

- 新增 `/查看预设` 指令：无参数时只列出所有预设名，不展示提示词；带预设名时显示该预设详情。
- 新增管理员指令 `/添加预设 [预设名] [提示词]` 与 `/删除预设 [预设名]`，支持在聊天侧维护极速宏预设并同步持久化配置。
- `/查看预设[预设名]` 紧凑写法也可查看单个预设详情，并兼容 `[预设名]` 这类括号写法。
- `/极速宏` 保留为兼容别名，效果等同于 `/查看预设`。

## 3.3.17

- 支持 /画 @用户 自动使用头像作为参考图（详见pr#33）

## 3.3.16

- Chat/Vision 与自定义 Chat Completions 生图通道不再给指令、LLM 生图或自拍额外拼接英文生图助手提示词和 `Prompt:` 前缀，实际请求只发送上游生成的原始提示词。
- 生成耗时和请求模型只在 `/画`、宏指令、`/自拍`、`/视频` 指令成功返回时显示，LLM 工具自动生成图片或视频时不再显示。

## 3.3.15

- 成功结果中的生成耗时统一显示为纯秒数 `s`，例如 `65.2s`，不再自动换算成分钟。

## 3.3.14

- 新增成功结果信息开关，可独立控制生成耗时和请求模型是否显示在图片、自拍、宏指令与视频成功返回中。
- 请求模型显示会优先采用指令或 LLM 工具透传的 `model` 参数，避免与实际请求模型不一致。
- 图片、自拍、宏指令成功耗时按从开始处理到成功下发的总耗时统计；视频按后台任务总耗时统计。

## 3.3.13

- 简化 Provider 请求体日志：图片 data URL、长文本和嵌套请求内容只输出摘要，避免日志刷屏。
- 标准、生图对话透传和自定义完整路径通道共用安全日志摘要，不再打印完整 Base64 图片内容。

## 3.3.12

- 图像 Provider 新增 `custom_endpoint` 自定义完整路径模式，配置页显示为“自定义”。
- 自定义模式只请求填写的完整 URL，不再自动追加 `/v1`、`/images/generations`、`/images/edits` 或 `/chat/completions`。
- 增强图像返回解析，兼容 OpenAI Images、Chat Completions、Responses API 以及常见中转站的 URL/Base64 返回格式。

## 3.3.11

- 新增“可使用人员白名单”：留空时默认不启用；填写后仅名单内用户可调用插件指令和 LLM 工具。
- 保留原“用户白名单（不限次数）”语义，仅用于豁免每日额度，不再承担使用权限限制说明。

## 3.3.10

- 新增指令失败回复配置，支持自定义 `/画`、`/自拍` 和宏指令的报错提示。
- 失败文案与等待文案统一纳入 `reply_config`，原生配置页和 Pages WebUI 均可编辑。

## 3.3.9

- 新增图片缓存清理：WebUI 可一键清理 `temp_images` 与 `user_refs` 中的图片缓存，且不会触碰人设参考图。
- 新增定时清理与容量上限自动清理配置，原生配置页和 Pages 配置页均可设置。
- 新增管理员指令 `/清理缓存`，用于聊天侧手动释放临时图片缓存。

## 3.3.8

- 生成调度新增图像生成、人设自拍、视频渲染备用节点开关，Pages 配置页可按点击顺序多选备用节点。
- 图像与自拍链路继续按配置顺序兜底，视频渲染现在也会在主节点失败后逐个切换备用视频节点。
- 原生配置页链路说明补充主节点/备用节点顺序规则，并对重复节点做去重处理。

## 3.3.7

- 新增指令回复文案配置，支持自定义 `/画`、`/自拍` 与宏指令开始生成前的等待回复。
- 原生配置页和 Pages 配置页共用 `reply_config`，并内置默认文案与常用占位符。

## 3.3.6

- 合并旧版权限管理与每日额度系统：`allowed_users` 现在作为用户白名单使用，命中后不限次数。
- 新增用户黑名单、群组白名单和统一的权限优先级：黑名单禁止使用，用户/群白名单豁免限额，其余用户默认可用并按每日额度统计。
- 新增 `/签到` 指令，受限用户可每日签到获得额外生图额度，支持固定值或随机区间配置。
- Pages 用量面板展示用户签到奖励、签到时间、群号与权限层级，原生配置页和 Pages 配置页均可配置新权限项。

## 3.3.5

- 新增按 AstrBot 用户 ID 统计的每日生图限额，可在原生配置页和 Pages 配置页同时配置。
- 新增当日生图统计 API 与 Pages 用量面板，展示当天总张数和每个用户的生图张数。
- `/画`、`/自拍`、宏指令以及 LLM 生图工具会在成功产图后计入当日统计，并在超出限额时阻止继续生图。

## 3.3.4

- 修复 AstrBot 原生配置页在人设列表内部点击“管理文件”时提示 `Config item not found or not file type` 的问题。
- 避开 AstrBot 当前不支持的 `template_list` 内嵌 `file` 结构，改为“当前激活人设参考图上传入口”负责原生上传，并自动同步到当前 active persona。
- 增强 `files/...` 原生上传路径解析，确保原生配置页上传的参考图能被生图链路作为本地图片正常读取。

## 3.3.3

- 恢复 AstrBot 原生插件配置入口，`_conf_schema.json` 现在覆盖 Pages 配置页可编辑的权限、链路、Provider、人设、副脑、预设和调试配置。
- 新增原生配置与 Pages 配置的单源同步策略：Pages 保存会同步写回 AstrBot 原生配置；插件启动或检测到原生配置保存后会同步到运行态和本地持久化备份。

## 3.3.2

- 修复配置页临时预览 URL 被保存进多组人设配置，导致刷新后显示“1 图”但预览空白的问题。
- 保存配置时会把当前有效的预览 token 反解为真实本地图片路径，并自动跳过历史遗留的失效预览 token。

## 3.3.1

- 修复多组人设中较大的本地参考图刷新后只显示数量、不显示预览的问题。
- 大图预览改为通过受控 token API 回传，避免把本地路径直接交给浏览器。

## 3.3.0

- 新增多组人设档案，支持每个人设独立维护 Base Prompt 与参考图组。
- 新增 `/人设` 与 `/切换人设 [序号/ID/名称]`，可在指令侧热切换自拍人格。
- 配置页新增人设组管理，切换人设时同步切换对应参考图矩阵。
- 保持旧版单人设字段兼容，旧配置会自动迁移为第一组人设。

## 3.2.1

- 深度优化提示词副脑，强制成功优化结果使用英文输出。
- 精进内置风格矩阵，尤其强化“自拍专用极致真实”的日常手机自拍质感。
- 增加人体结构、自拍角度、皮肤质感、反光控制、畸形规避等真实感护栏。

## 3.2.0

- 统一插件版本、作者和 metadata 注册信息。
- 按 AstrBot 数据目录规范持久化配置与参考图。
- 增强配置归一化，兼容旧模型逗号写法和多参考图。
- 修复副脑链路保存位置、空 API Key 崩溃、视频参数丢失和 URL 拼接问题。
- 增加后台视频任务生命周期清理。
- 修复配置页潜在 HTML 注入、预设冒号截断和桌面端卡片样式缺失。

## 1.1.2

- 适配手机端配置页。
